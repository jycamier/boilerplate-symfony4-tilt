# dc
docker_compose('docker-compose.dev.yml')
docker_build('clever/symfony/php7.4-fpm-alpine:dev', './',
  live_update=[
    sync('./', '/app'),
    run('composer install --working-dir=/app', trigger = 'composer.lock'),
  ],
  target = 'symfony_dev'
)
